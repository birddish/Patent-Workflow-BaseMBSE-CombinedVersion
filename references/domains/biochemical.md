# 生化/材料领域增量规则

只在总控确认技术领域为生化或材料后加载。区分组成、制备、条件、步骤、表征、范围和效果；缺少实验、表征或来源依据时保留 `[Q]`、`pending_review` 或 `conditional`，不得生成未经证实的数值、效果或范围。

所有证据仍必须先存在主 JSON 的来源快照或规范化事实中；本领域规则不允许在 JSON-only 事务中回读外部实验记录或文献文件。
